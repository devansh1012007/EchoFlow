# VCS
.git
.gitignore

# Python
__pycache__
*.pyc
*.pyo
*.egg-info
dist
build
*.egg

# Virtual environments
venv
.venv
env
.env

# Node
node_modules
frontend/node_modules
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# IDE
.vscode
.idea
*.swp
*.swo
*~

# Media & static (mounted as volumes in Docker)
media
staticfiles
cache

# Frontend (built/served separately, not needed in backend images)
frontend

# NOTE: wheelhouse/ MUST stay in the build context — the Dockerfile COPYs it.

# Docker
.dockerignore
Dockerfile*
docker-compose*.yml

# Docs & seed data (not needed in production image)
docs
Startup_related_docs
backend/scripts
hidden folder
.cache/pip/

# Test artifacts
*.log
*.db
*.sqlite3

# OS
.DS_Store
Thumbs.db
